<!--
  Extensions activated only when com.intellij.modules.java is present (IntelliJ IDEA, Android Studio).
  Keeps geai loadable in non-JVM IDEs (PyCharm, GoLand, etc.) while enabling full PSI-based
  structure navigation (classes, methods, inheritance) when the Java plugin is available.
-->
<idea-plugin>
</idea-plugin>
